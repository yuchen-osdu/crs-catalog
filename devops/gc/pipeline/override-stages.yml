variables:
  GC_SERVICE: crs-catalog
  GC_VENDOR: gc
  GC_INT_TEST_TYPE: python
  GC_PYTHON_INT_TEST_SUBDIR: testing/catalog_test_gc
  # --- osdu-anthos specific variables ---
  GC_ANTHOS_PYTHON_INT_TEST_SUBDIR: testing/catalog_test_anthos
  # --- end of osdu-anthos specific variables ---
  # FIXME remove when all services are migrated to a single helm
  GC_ENABLE_HELM_CONFIG: "false"

gc-test-python:
  variables:
    VIRTUAL_SERVICE_HOST_NAME: $GC_CRS_CATALOG_HOSTNAME

gc-dev2-test-python:
  variables:
    VIRTUAL_SERVICE_HOST_NAME: $GC_CRS_CATALOG_HOSTNAME

gc-anthos-test-python:
  variables:
    VIRTUAL_SERVICE_HOST_NAME: $OSDU_ANTHOS_CRS_CATALOG_HOSTNAME
