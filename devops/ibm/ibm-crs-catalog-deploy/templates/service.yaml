apiVersion: v1
kind: Service
metadata:
  name: {{ include "ibm-crs-catalog-deploy.fullname" . }}
  labels:
    {{- include "ibm-crs-catalog-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ibm-crs-catalog-deploy.selectorLabels" . | nindent 4 }}
