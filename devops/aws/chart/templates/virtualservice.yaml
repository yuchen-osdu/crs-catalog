{{- template "common.virtualservice" (list . "os-crs-catalog.virtualservice") -}}
{{- define "os-crs-catalog.virtualservice" -}}
## Define overrides for the service's virtual service resource here
{{- end -}}