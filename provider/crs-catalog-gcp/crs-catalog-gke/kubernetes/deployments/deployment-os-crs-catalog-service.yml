apiVersion: v1
kind: Service
metadata:
  name: crs-catalog-service
  labels:
    app: crs-catalog-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: crs-catalog-service
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: crs-catalog-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: crs-catalog-service
        version: v1
    spec:
      containers:
        - name: crs-catalog-service
          envFrom:
            - configMapRef:
                name: os-service-config
          image: gcr.io/opendes/crs-catalog-app
          imagePullPolicy: Always
          volumeMounts:
            - name: shared-data
              mountPath: /mnt/crs_catalogs
          ports:
            - containerPort: 8080
      initContainers:
        - name: "init-crs-catalog"
          image: "{{ .Values.catalogs.repository }}:{{ .Values.catalogs.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          volumeMounts:
          - name: shared-data
            mountPath: /mnt/crs_catalogs